# macOS
.DS_Store

# Xcode build output & per-user state (never needed to build)
DerivedData/
build/
*.xcuserstate
xcuserdata/

# Swift Package Manager build artifacts
.build/

# Intentionally committed for a zero-setup clone/build/run:
#   - the .xcodeproj / .xcworkspace   (opens & builds as-is)
#   - GoogleService-Info.plist        (Firebase config)
#   - Package.resolved                (pinned dependency versions)

# Xcode-generated per-user SwiftPM package state
.swiftpm/
